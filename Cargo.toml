[package]
name = "link_checker"
version = "0.1.0"
edition = "2024"

[dependencies]
regex = "1"
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls"] }
scraper = "0.20"
tokio = { version = "1", features = ["full"] }
